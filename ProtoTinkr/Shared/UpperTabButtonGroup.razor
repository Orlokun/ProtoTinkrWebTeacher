@using Syncfusion.Blazor.Buttons

<SfButtonGroup CssClass="e-round-corner" Mode="SelectionMode.Single">
    @foreach (var buttonText in @buttons)
    {
        <SfButton>@buttonText</SfButton>
    }
</SfButtonGroup>

@code
{
    [Parameter]
    public List<string> buttons { get; set; }
}